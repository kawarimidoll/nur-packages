name: Update Sources

on:
  pull_request:
    paths:
      - 'nvfetcher.toml'

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true # required for git push

      - uses: cachix/install-nix-action@2126ae7fc54c9df00dd18f7f18754393182c73cd # v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run nvfetcher
        run: nix run .#nvfetcher

      - name: Run sync-readme
        run: nix run .#sync-readme

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add _sources/ README.md
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update sources and readme"
            git push
          fi
